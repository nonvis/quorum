# Quorum — Core Concepts

Concepts (modes, agents, vaults, HANDOFF protocol, daemon, archetypes) are documented in the project's design vault — single source of truth, kept current as architecture evolves.

**Source-of-truth notes (private vault):**
- `01 - Architecture.md` — daemon components, agent invocation flow, archetypes, knowledge accumulation
- `12 - Execution Modes.md` — generic vs brainstorm modes, write surfaces, the engine axis
- `99 - Quorum Manual.md` — end-to-end setup + operation guide
- `06 - Decision Log.md` — every architectural decision with rationale and date

**In-repo specs:** `templates/specs/autopilot-protocol.md` (autopilot supervisor + flight plan + checkpoint).

This file used to contain prose copies of those concepts. Removed to prevent doc drift between repo and vault. The vault is the canonical reference.
